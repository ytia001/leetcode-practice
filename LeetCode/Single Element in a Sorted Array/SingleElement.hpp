#include <vector>
#include <iostream>

class Solution {
public:
    int singleNonDuplicateV1(std::vector<int>& nums);
    int singleNonDuplicate(std::vector<int>& nums);
    bool checkSingle(std::vector<int>& nums,int target);
};