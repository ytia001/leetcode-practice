#include <vector>
#include <iostream>

class Solution {
public:
    int shipWithinDays(std::vector<int>& weights, int days);
    int totalnumberDays (std::vector<int>& weights, int minWeight );
     int shipWithinDaysSoln(std::vector<int>& weights, int days);
    int totalnumberDaysSoln (std::vector<int>& weights, int minWeight );
};