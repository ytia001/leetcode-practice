#include <vector>
#include <iostream>

class Solution {
public:
    int mincostTickets(std::vector<int>& days, std::vector<int>& costs);
    int binarySearch(int target,const std::vector<int>& days);
};